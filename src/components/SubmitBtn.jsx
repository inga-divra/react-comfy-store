
const SubmitBtn = ({ text }) => {
    return (
        <button
            className="btn btn-primary uppercase"
            type="button">
            {text}
        </button>
    )
}

export default SubmitBtn